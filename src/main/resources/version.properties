# Filtered by the maven-resources-plugin at build time (see pom.xml <resources>) -
# this is how Main.VersionProvider reads the CLI's --version string without
# hardcoding a copy of the pom.xml version that could drift out of sync.
version=${project.version}
