package com.phishnet.cli;

/** How much the CLI should print. QUIET = one machine-parsable line, NORMAL = summary, VERBOSE = summary + details. */
public enum OutputLevel {
    QUIET,
    NORMAL,
    VERBOSE
}
